<p style="color: green"><%= notice %></p>

<h1>Peeps</h1>

<div id="peeps">
  <% @peeps.each do |peep| %>
    <%= render peep %>
    <p>
      <%= link_to "Show this peep", peep %>
    </p>
  <% end %>
</div>

<%= link_to "New peep", new_peep_path %>
